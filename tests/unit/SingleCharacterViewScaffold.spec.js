/* TODO: Ejercicio! */
describe('SingleCharacterView', () => {
  it('should call the action to set the current character', () => {});

  describe('before character data has loaded', () => {
    it('renders a loading indicator', () => {});
  });

  describe('when character data has been loaded successfully', () => {
    it('renders a heading with the name', () => {});

    it('renders the image', () => {});

    it('renders the specie', () => {});

    it('renders the gender', () => {});

    it('renders the origin', () => {});

    it('renders the status', () => {});

    it('renders the location', () => {});
  });
});
